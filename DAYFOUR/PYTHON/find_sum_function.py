def sumArray (arrayNumber, regularNumber):

	newArray = []
	for indices in range(len(arrayNumber)):
		for index in range(len(arrayNumber)):
			if arrayNumber[indices] + arrayNumber[index] == regularNumber:
				newArray.append = (arrayNumber[indices])
				newArray.append = (arrayNumber[index]) 
				return newArray
				
	return newArray


